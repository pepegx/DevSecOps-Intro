# Lab 10 Setup Notes

This directory keeps only compact setup evidence for the PR:

- `compose-check.txt`
- `docker-compose-ps.txt`

Local-only materials are intentionally ignored so the lab submission stays reviewable:

- `django-DefectDojo/` upstream checkout
- `defectdojo-*.tar.gz` image archives

The ignored files are still useful for local reruns, but they are not required deliverables for `labs/lab10.md`.
